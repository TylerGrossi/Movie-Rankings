# Pinned to the versions currently working on the owner's machine.
# Install:  pip install -r requirements.txt

# Core pipeline (enrichment + prediction)
requests==2.32.5
pandas==2.3.3
numpy==2.4.1
openpyxl==3.1.5
python-dotenv==1.2.1
scikit-learn==1.8.0
joblib==1.5.3
xgboost==3.1.3

# Only needed for src/movies/imdb_api_pull.py (Selenium IMDb export)
selenium==4.40.0
webdriver-manager==4.0.2
